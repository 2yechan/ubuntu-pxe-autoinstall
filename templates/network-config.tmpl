# Intentionally empty, and served with 200 for the same reason as vendor-data:
# cloud-init retries a missing network-config ten times before giving up.
# An empty document means "no override" -- the installed system keeps
# subiquity's default DHCP configuration.
